import React from 'react';

import TeamProfilePageContainer from '../../containers/team_profile_page_container';

const TeamProfilePage = (props) => {

    return (
        <div>
            <TeamProfilePageContainer {...props}/>
        </div>
    );
}

export default TeamProfilePage;
