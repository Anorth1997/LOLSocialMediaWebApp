import React from 'react';

import FooterContainer from '../../containers/footerContainer';

const Footer = (props) => {
    return (
        <div>
            <FooterContainer />
        </div>
    );
};

export default Footer;