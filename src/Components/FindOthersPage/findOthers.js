import React from 'react';

import FindContainer from '../../containers/find_others_container';

const Find = (props) => {
    return (
        <div>
            <FindContainer />
        </div>
    );
}

export default Find;
