import React from 'react';

import LoginContainer from '../../containers/login_container';

const Login = (props) => {
    return (
        <div>
            <LoginContainer />
        </div>
    );
}

export default Login;
