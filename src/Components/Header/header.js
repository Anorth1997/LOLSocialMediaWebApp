import React from 'react';

import HeaderContainer from '../../containers/headerContainer';

const Header = (props) => {
    return (
        <div>
            <HeaderContainer />
        </div>
    );
};

export default Header;