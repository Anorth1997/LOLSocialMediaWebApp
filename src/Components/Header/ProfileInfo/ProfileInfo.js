import React from 'react';

import ProfilePictureContainer from '../../containers/Reusables/profile_picture_container';

const ProfileInfo = (props) => {
    return (
        <div>
            <ProfilePictureContainer />
        </div>
    );
}

export default ProfileInfo;
