pipeline {
    agent none
    triggers {
        GenericTrigger(
             genericVariables: [
              [key: 'ref', value: '$.ref']
             ],
             genericRequestVariables: [
              [key: 'X-GitHub-Event', regexpFilter: 'pull-request'],
              [key: 'headerWithNumber', regexpFilter: '[^0-9]'],
             ],
             genericHeaderVariables: [
              [key: 'X-GitHub-Event', regexpFilter: 'pull-request'],
              [key: 'headerWithNumber', regexpFilter: '[^0-9]'],
             ],

             causeString: 'Triggered on the branch $ref with $x_github_event',

             token: 'abc123',

             printContributedVariables: true,
             printPostContent: true,

             silentResponse: false,

             regexpFilterText: '$ref',
        )
    }
    stages {
        stage('check trigger vars') {
            steps {
                echo "$ref"
                echo "j"
            }
        }

        stage('build') {
            agent { docker { image 'node:12.13.0-alpine' } }
            steps {
                sh 'npm --version'
                sh 'node --version'
                sh 'ls'
            }
        }

        stage('Staging approval') {
            steps {
                input "Push to staging??"
            }
        }

        stage('Staging') {
            steps {
                echo "staging"
            }
        }
        
        stage('Production approval') {
            steps {
                input "Push to production?"
            }
        }
        
        stage('Deploy') {
            steps {
                echo "Deploying"
            }
        }    
    }
    post {
        success {
            echo 'Job Succeed'
        }
        failure {
            echo 'Job Failure'
        }
    }
}
